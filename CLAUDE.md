# CLAUDE.md — kentkim (작가 사이트)

**이 레포로 작업을 시작할 때 가장 먼저 이 파일을 읽을 것.**

## 1. 이 사이트가 무엇인가

**김형섭(Kent Kim)의 작가 사이트다.** 주 독자는 해외 갤러리·큐레이터·문화원이고,
목적은 그림 작가로서의 커리어다. 파는 사이트가 아니다.

- `index.html` 첫 화면은 **그림 한 점이 화면을 가득** 채운다(대표님 결정, 2026-08-22).
  설명보다 작품이 먼저다.
- `places.html` **《133 Places》 지도.** 이게 이 사이트의 핵심 기능이다.
  유튜브 링크 133개를 나열한 형태로는 아무도 안 본다. 문화원 지원서와 Saatchi Art
  프로필에서 **이 지도 주소 하나로 자료 전체를 대신한다.**
- `about.html` 이력·소개·문의.

## 2. ★ 아카이브 사이트와의 관계 — 링크는 한 방향만

어머니 화랑 아카이브는 **별개 레포·별개 도메인**이다 → `kentkim0326/haena-kent`
(https://haena-kent.vercel.app).

**나눈 이유**: 유산 70점을 300곳 갤러리에 알리는데 운영자가 작가이기도 하면
**작가-딜러 이해충돌**로 읽힌다. 상대는 「유족 대리인인가, 자기 작품 팔러 온 작가인가」를
1초 만에 판단하고 거기서 지면 나머지를 안 읽는다.

- **이 사이트 → 아카이브: 링크를 건다**(`about.html` 화랑 섹션). 열여섯에 어머니와
  화랑을 만든 것은 그의 이력이고, 큐레이터에겐 강한 서사다.
- **아카이브 → 이 사이트: 링크를 걸지 않는다.** 그쪽에는 관리자 이름만 적혀 있다.
  **그쪽에 이 주소를 넣지 말 것.**
- **두 사이트는 생김새도 달라야 한다.** 아카이브는 종이색 기관 문서, 이쪽은 어두운
  바탕에 작품이 화면을 먹는다. 같은 얼굴이면 도메인만 다를 뿐 눈에는 한 사이트다.

## 3. 절대 규칙 (어기면 되돌리기 어려움)

1. **백남준** — 허용되는 표현은 이것뿐. **국내 공무원 상대에서 특히 조심한다** —
   부풀리고 싶은 유혹이 가장 큰 자리이고, 기자가 한 번 「사사」로 쓰면 영구히 남는다.
   「어머니 화랑에 드나든 작가 중 한 사람이었고, 10대 때 만났다.」
   `studied under` · `mentor` · 사사 · 지도 · 제자 → **전부 금지.**
   검증되는 순간 나머지 이력까지 같이 의심받는다.
2. **이강소는 언급하지 않는다 — 국내 축제 제안서에서도 마찬가지다**(2026-09-03 재확인).
   대표님이 「집안이 미술 집안이라는 것을 어필하면 어떤가」로 다시 물으셨고, 답은
   **집안 서사는 쓰되 외삼촌 이름만 뺀다**로 정했다. 이유 셋:
   ① 남의 이름이다 — 담당자가 확인 전화를 하면 그쪽이 곤란해진다.
   ② **되돌릴 수 없다** — 축제 보도자료에 「이강소 화백의 조카」로 한 번 나가면
      수식어가 따라붙고, 해외 자료에서 빼기로 한 결정과 정면으로 부딪힌다.
      이름은 나중에 언제든 더할 수 있지만 한 번 나간 것은 못 거둔다.
   ③ 없어도 충분하다 — 나머지가 전부 본인 이력이라 더 세다.
   **써도 되는 집안 서사**(전부 검증 가능하고 본인 이력이다): 어머니 이강자가
   서울대에서 조소를 전공하고 뉴욕 소호 568 Broadway 에서 화랑을 운영했다는 것,
   **고등학생 때 그 화랑을 어머니와 함께 만들었다**는 것, 고등학생 때 뉴저지 주 대표로
   2년 연속 선정돼 모리스 박물관에서 전시했다는 것, 1992년 뉴욕 전시.
3. **모리스 박물관** — 전시명과 정확한 연도가 아직 미확인이다. 지금 문장
   (「1990·1991 뉴저지 주 대표 선정, 모리스 박물관 전시」) 이상으로 쓰지 않는다.
   실물 증빙(기사·상장·도록)을 확보하면 그때 늘린다.
4. **기금 마련·모금 문구 금지.** 해외에서는 작품이 아니라 모금 활동으로 읽힌다.
5. **돈 내는 해외 전시·그룹전 「초대」는 넣지 않는다.** 전부 참가비 장사이고 이력에
   도움이 안 된다.
6. **확인되지 않은 것은 쓰지 않는다.**

## 3-1. ★ 출처 문서 — 「KENT KIM — ARTIST MATERIALS (EN)」 (2026-08-20)

사이트 영문의 상당수가 이 문서에서 왔다(대표님 소유 구글 문서, 비공개).
**문장을 고칠 때 먼저 이 문서를 본다.** 문서 자체에 확인·주의사항이 붙어 있고
이 CLAUDE.md 3절과 같은 내용이다.

이 문서에서 확정된 것:
- **133점은 진행 중이 아니라 완성됐다.** 2023년 9월 ~ 2024년, 현장에서 133점,
  첫 획부터 서명까지 촬영한 영상 133편. 사이트를 「ongoing」 으로 쓰면 틀린다.
- 재료는 **검정·흰색·은색·금색** 넷뿐. 한자리에서 빠르게, 사람들 앞에서 그린다.
- 크기 **33 × 42 cm ~ 100 × 100 cm**, 액자 없이 나무틀에 그대로.
- **「133」은 장소 수가 아니라 그림 수다.** 한 장소에서 여러 점을 그렸다
  (가로수길 5점 · 호미곶 5점 · 포항시청 3점 …). 그래서 `places-data.js` 가 두 층이다 —
  `WORKS`(그림) 가 `PLACES`(장소) 를 id 로 가리킨다. **「133개 장소」라고 쓰면 틀린다.**
- 그림마다 화폭에 쓴 말이 있다 → `WORKS` 의 `word`. **대표님이 적어 준 그대로 두고 고치지 않는다.**
- 하버드 경제학, 이후 이십 년 저술·방송·작곡, 동아일보 일러스트 칼럼 연재,
  2023년 전업 복귀.

**★ 확인이 필요한 것 둘 — 사이트에 아직 안 넣었다:**
1. ~~저서 권수~~ — **23권으로 확정**(대표님 2026-09-03 재확인). `about` 본문에 넣었다.
   ARTIST MATERIALS 문서의 「eighteen books」는 낡은 숫자다.
2. **휴대전화 번호와 학교 주소는 넣지 않았다.** 문서 서명란에 있으나 공개 웹페이지에
   개인 휴대번호를 올리면 스팸 표적이 된다. 문의는 메일 하나로 받는다.
   (학교는 CLAUDE.md 3절 4번 — 모금으로 읽히므로 미술 자료에 쓰지 않는다.)
3. **ELEGERE 는 2016년 제작 · 2017년 입상**(AOF 영화제 호러 부문, 미국). 대표님 확인, 2026-09-03.
   **영화라서 미술 사이트에는 넣지 않았다** — 넣을 일이 생기면 연도를 이렇게 나눠 적는다.
4. 원문의 「Gyeongpodae, Sokcho, Donghae, Samcheok, Pohang City Hall — Steel」 은
   Steel 이 다섯 곳 전부인지 포항 하나인지 갈린다. **포항에만 넣었다.** 확인 필요.

## 3-2. ★ 축제·행사 제안 페이지 (`performance.html`)

**주 독자가 다르다.** 나머지 세 페이지는 해외 큐레이터·문화원용이지만 이 페이지는
**국내 축제 주최 측(지자체 담당 공무원)** 이 읽는다. 한국어 브라우저는 자동으로 한국어로
뜨므로 별도 사이트를 만들 필요가 없다.

**파는 것을 「부대 코너」가 아니라 「개막 퍼포먼스 + 작품 1점」으로 잡았다.** 이유:
- 「재미난 코너」는 부대행사 예산에 들어간다 — 작고, 먼저 잘리고, 버스킹과 같은 칸이다.
- **개막식은 이미 예산이 잡혀 있는 순서**이고, **작품이 물리적으로 남으면** 청사에 걸고
  홍보물에 쓸 수 있어 「용역」이 아니라 「작품 제작」으로 결재가 올라간다.
- 담당자가 가장 두려워하는 것은 「당일에 못 하면」이다. **133점을 전부 야외에서 그렸고
  전 과정이 영상으로 남아 있다**는 사실이 그 걱정을 없앤다 — 제안의 절반이 이것이어야 한다.

**규칙:**
- **가격을 적지 않는다.** 「규모·작품 수·캔버스 크기를 함께 정하고 비용은 거기에 따른다」까지만.
  숫자를 박으면 그것이 천장이 되고, 지자체끼리 비교 대상이 된다. 견적은 메일로 개별 발송.
- **못 하는 것을 약속하지 않는다.** 우천 대안·소요 시간·필요 공간을 미리 적어 둔 것은
  담당자가 식순을 짜야 하기 때문이다. 실제로 지킬 수 있는 숫자만 적는다.
- **관공서 계약 관련 문장은 「통상적인 방식으로 처리한다」까지만.** 수의계약 한도 같은
  규정을 사이트에 단정해 적지 않는다 — 지자체마다 다르고 바뀐다(3절 6번).

## 4. 작품 캡션 형식

```
Title (제목)
Acrylic on canvas
53 × 53 cm
2023
```

## 5. 구조

빌드 도구 없음. `git push` 하면 Vercel 이 그대로 올린다.

```
index.html      첫 화면(그림 한 점) + 작품 그리드 + 인용
places.html     133 Places — 지도 + 장소 목록 + 그 프로젝트의 그림
performance.html 축제·행사 개막 퍼포먼스 제안 (국내용. 3-2 절 참고)
about.html      소개 · 이력 · 화랑(아카이브 링크) · 문의
styles.css      어두운 바탕 + 금색 하나
i18n.js         ★ 한국어 문장만 (영어는 HTML 안에 있다)
app.js          언어 전환 · 이미지 지연 삽입 · 133 Places 렌더
map.js          Leaflet 지도 (못 불러오면 조용히 물러나고 목록이 대신한다)
places-data.js  ★ 133개 장소 좌표·영상·사진. 여기만 고치면 지도·목록·집계가 따라온다
assets/         works/ places/ incoming/
```

확인은 서버로 띄운다 (`file://` 는 스크립트가 막힌다):

```bash
python3 -m http.server 5173
```

### ⚠️ 함정 — 「`main` 에 올렸는데 사이트에 안 나온다」 (2026-09-03)

**Vercel 이 배포하는 브랜치를 먼저 확인한다.** 이 프로젝트는 처음 Import 될 때
GitHub 기본 브랜치가 `claude/art-portfolio-site-mi0oj9` 였던 탓에, Vercel 의
**Production Branch 가 그 브랜치로 잡혀 있었다.** `main` 에 아무리 푸시해도
사이트에는 반영되지 않았고, `hero.mp4` 가 **404** 로 떴다 — 파일은 `main` 에만
있었기 때문이다. 원인을 찾는 데 세션 하나를 다 썼다.

- **증상이 이러면 이 함정이다**: 커밋·푸시는 분명히 됐는데 사이트가 옛날 그대로,
  또는 새로 올린 파일만 404. Vercel 배포 목록에 뜬 **커밋 메시지가 내가 방금
  올린 것이 아닌지** 본다 — 그게 곧 배포 중인 브랜치다.
- **확인**: `git remote show origin` 의 `HEAD branch`(=GitHub 기본 브랜치) 와
  Vercel **Settings → Environments → Production → Branch Tracking** 을 맞춰 본다.
  (구 UI 는 Settings → Git 에 Production Branch 가 있었으나 **신규 UI 는
  Environments 로 옮겼다** — Git 페이지를 아무리 뒤져도 없다.)
- **지금 상태**: GitHub 기본 브랜치는 `main` 으로 고쳤다. Vercel Production Branch 도
  `main` 으로 맞춘다. **옛 브랜치는 Vercel 설정을 바꾸고 배포를 확인한 뒤에 지운다** —
  먼저 지우면 프로덕션 공급원이 사라진다.

### ⚠️ 히어로 영상 — 순서를 되돌리지 말 것

`tryHeroVideo()` 의 순서는 **① `plate` 에 붙이고 ② `src` 를 주고 ③ 곧바로 `play()`** 다.
둘 다 사파리 때문에 이렇게 돼 있다:

- **떼어 놓은 `<video>` 에 `src` 만 주면** 사파리에서 로드가 시작되지 않는다 → 먼저 붙인다.
- **`loadeddata` 를 기다렸다가 `play()` 하면** iOS 에서 영영 안 걸린다. iOS 사파리는
  `preload="auto"` 를 무시하고 **재생을 시도할 때까지 받지 않기** 때문에, 기다리는 쪽이
  이벤트를 영원히 못 받는다 → 바로 `play()` 하고 실패하면(`catch`) 사진으로 남긴다.

`poster` 가 뒤에 깔린 사진과 같은 그림이라 붙여 두어도 재생 전에는 티가 나지 않는다.

### 영어가 HTML 안에 있다

**영문이 HTML 에 그대로 있고 `i18n.js` 는 한국어 대체 문장만 갖는다.**
크롤러·JS 꺼진 브라우저도 영문 전문을 읽는다. 주 독자가 해외라 이게 맞다.
문장을 고칠 때는 **HTML(영어)과 `i18n.js`(한국어) 두 곳**이다. 키 대조:

```bash
node -e "const fs=require('fs');const KO=eval(fs.readFileSync('i18n.js','utf8')+';KO');
const k=new Set();for(const f of ['index.html','places.html','about.html'])
for(const m of fs.readFileSync(f,'utf8').matchAll(/data-t=\"([^\"]+)\"/g))k.add(m[1]);
console.log('missing',[...k].filter(x=>!(x in KO)),'unused',Object.keys(KO).filter(x=>!k.has(x)));"
```

### 이미지 — 없으면 조용히 비어 있다

`data-img` 가 붙은 자리는 **파일이 실제로 있을 때만** 이미지를 넣는다.
파일만 올리면 코드를 안 고쳐도 뜬다.

| 경로 | 내용 |
|---|---|
| `assets/works/hero.jpg` | **첫 화면을 가득 채우는 한 점.** 가로로 넉넉한 사진(2400px 이상)이 좋다 |
| `assets/works/hero.mp4` | **index 첫 화면 영상.** 파일을 올리면 사진 위에 자동으로 얹혀 무음 반복 재생된다. 없으면 사진이 그대로 남는다 |
| `assets/works/buddha.mp4` | **places(133 Places) 첫 화면 영상** |
| `assets/works/peak2.mp4` | **performance(라이브 페인팅 제안) 첫 화면 영상** |
| `assets/works/work-01…06.jpg` | 작품 그리드 6점 |
| `places-data.js` 의 `img` | 133 Places 개별 작품 사진 |

**히어로 영상은 `hero.mp4` 를 올리기만 하면 켜진다.** `app.js` 의 `tryHeroVideo()` 가
파일이 실제로 있을 때만 `<video>` 를 만든다. 무음·반복·`playsinline` 이라 모바일에서도 돌고,
사진을 `poster` 로 깔아 두어 뜨기 전에도 빈 화면이 안 보인다. 자동재생이 브라우저에 막히면
영상을 지우고 사진으로 남는다. 「동작 줄이기」를 켠 사용자에게는 아예 넣지 않는다.
**권장 사양: 가로 1280~1920px · H.264 · 무음 · 8MB 이하.** 배경이라 화질보다 용량이 중요하다.

**★ 모바일에서도 영상을 보여 준다**(2026-09-03, 대표님 결정. 한 번 막았다가 되돌렸다).
`shouldSkipHeroVideo()` 가 거르는 것은 **「동작 줄이기」·데이터 절약 모드·2G/3G** 셋뿐이고,
**화면 크기로는 막지 않는다.** 사진(`hero.jpg`)이 뒤에 먼저 깔려 있어 버퍼링 중에도 빈 화면이
보이지 않는다 — 처음에 걱정했던 「첫 화면이 몇 초 빈다」는 실제로는 일어나지 않는다.

**⚠️ 사파리에는 Network Information API 가 없다.** 그래서 아이폰이 셀룰러인지 와이파이인지
**코드로는 알 수 없다.** 셀룰러에서 14MB 를 그대로 받게 되므로, 데이터 절약은 감지가 아니라
**가벼운 파일을 따로 주는 방식**으로 푼다:

```html
<div class="hero-img" data-img="…/hero.jpg"
     data-video="assets/works/hero.mp4"
     data-video-mobile="assets/works/hero-mobile.mp4"></div>
```

`heroVideoSrc()` 가 **≤640px 이고 `data-video-mobile` 이 있을 때만** 그 파일을 쓴다.
속성이 없으면 원본을 쓰므로 **파일을 올리기 전에도 그대로 동작한다.** 가벼운 파일 만들기:

```bash
avconvert --preset Preset640x480 --source 원본.mov --output hero-mobile.mp4
```

**네 페이지 중 셋이 영상으로 열린다**(2026-09-03, 대표님 결정):

| 페이지 | 영상 | 사진(poster·폴백) |
|---|---|---|
| `index.html` | `assets/works/hero.mp4` | `assets/works/hero.jpg` |
| `places.html` (133 Places) | `assets/works/buddha.mp4` | `assets/places/IMG_4814.jpg` |
| `performance.html` (라이브 페인팅 제안) | `assets/works/peak2.mp4` | `assets/places/IMG_2462.jpg` |
| `about.html` | **없음 — 사진만** | `assets/studio/show-02.jpg` |

**about 은 일부러 사진이다.** 넷 다 영상이면 무게가 없어진다.
**영상 파일이 없으면 그 페이지는 조용히 사진만 남는다** — 그래서 파일이 오기 전에 속성을
먼저 넣어 두어도 안전하다. `data-video` 한 줄만 바꾸면 페이지끼리 맞바꿀 수 있다.

**히어로 사진이 없으면 `.no-image` 로 바뀌어 글씨만으로 선다** — 빈 회색 사각형을
보이지 않는다. 그래서 사진이 오기 전에도 페이지가 부끄럽지 않다.
정리 안 된 사진은 `assets/incoming/` 에 그냥 올린다.

### 133 Places 지도

`places-data.js` 의 `PLACES` 배열이 전부다. 한 줄 넣으면 지도의 점·목록·
「지도에 표시된 곳」 숫자가 동시에 늘어난다. **숫자를 손으로 적지 않는다.**
지금 **12/133**. 좌표는 지명 기준 근사값이라 실제 작업 지점으로 바꾸면 그만큼 정확해진다.
영상이 있는 장소는 `video` 에 유튜브 주소를 넣으면 목록과 지도 팝업에서 바로 열린다.

## 6. 아직 안 한 것

- [x] ~~히어로 그림 한 점 + 작품 사진~~ — **완료**(2026-09-03). 대표님이 133장을 올려 주셨다.
      히어로는 `IMG_5255`(권금성 정상 · 등산객 · 「PEAK」) — 프로젝트 전체가 한 장에 들어 있어 골랐다.
      그리드 6점은 성산일출봉 START · 권금성 PEAK · 강남 황소상 · 풍력발전기 · 그리는 장면 · 해변.
      ※ 히어로에 **어두운 스크림과 글자 그림자**를 넣었다 — 안 넣으면 밝은 하늘에서 내비와 제목이 사라진다.
        사진을 바꿔도 읽히도록 CSS 로 처리해 뒀으니 파일만 갈아 끼우면 된다.

- [ ] **★ 133장을 그림 번호에 짝짓기 — 지금 11점 연결됨**
      (104 START · 114 권금성 · 115 Peak · 116 경포대 · 119 삼척 · 122 Pohang · 123 鐵 ·
       125 East · 126 개벽 · 127 바다 · 128 Ocean).
      **화폭에 쓴 낱말이 사진에서 읽히고 배경 지형까지 맞는 것만 넣었다.** 추측으로 잇지 않는다.
      ※ **119번의 낱말을 사진으로 확정했다** — 화폭에 「삼척」이라 쓰여 있다.
        원문이 「119번째 그림: 삼척」 한 토막이라 비워 뒀던 자리다.
      ※ **폴더에는 세 종류가 섞여 있다**(대표님 확인, 2026-09-03):
        ① 133 Places 현장 사진 ② **갤러리 전시 사진**(SUN·EMC·PLATO·전시장 전경, 2023 개인전)
        ③ 작업실 사진. 그래서 **133장 ≠ 133점**이고, 한 작품을 여러 각도로 찍은 것도 여럿이다.
      ※ **파일명 순서는 그림 번호와도 촬영 순서와도 무관하다**(코드로 확인).
      → 짝짓는 방법: **사진을 4×4 콘택트시트로 묶어 훑고**, 화폭의 낱말을 `WORKS` 의 `word` 와 맞춘다
        (`PIL` 로 시트를 만들면 133장을 9장으로 볼 수 있다).
      → **나머지 101점 목록(장소·낱말·영상)을 받으면 훨씬 빨라진다.**

⚠️ **함정 — `.place-group` 에 `padding:0` 을 반드시 둔다.** 장소 묶음을 `<section>` 으로 만드는데
전역 `section{padding:clamp(64px,10vw,140px) 0}` 이 그대로 먹어서 묶음마다 위아래 140px 씩
벌어진다. 실제로 한 번 겪었다(묶음 높이 98px 이 378px 로 나왔다).
- [ ] **나머지 101점** — 지금 **32/133**(102~133번)이 들어가 있다. 대표님 목록을 그대로
      `WORKS` 에 이어 붙이면 된다. 형식은 「N번째 그림: 장소: 낱말 + 유튜브 주소」.
      **장소가 새로 나오면 `PLACES` 에 줄을 하나 먼저 만들고 좌표를 넣는다.**
- [ ] **확인 필요한 낱말 넷** — 「위대할 태」(太의 새김) · 「철 (한자)」 · 「산」 · 「석가모니」 의
      영어 뜻풀이를 비워 뒀다. **119번은 원문이 「119번째 그림: 삼척」 한 토막이라 낱말 자체가 비어 있다**
      (사이트에 「낱말 확인 중」으로 나온다)
- [ ] 작품 캡션(제목·재료·치수·연도) — 사진과 함께 채운다
- [ ] Vercel 프로젝트 연결, 도메인
- [ ] Saatchi Art 프로필 (등록비 없음, 원화 수수료 40%)
- [ ] 해외 한국문화원 1차 8곳: 뉴욕(최우선) · 런던 · 파리 · 베를린 · LA · 워싱턴DC ·
      도쿄 · 로마. **연락처는 korean-culture.org 에서 직접 확인한다 — 주소를 추측해
      만들지 말 것**(과거 오발송 사고 있음)
